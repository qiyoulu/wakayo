# local python env
.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# local sqlite store (machine-local, not in the repo)
memory.db
*.db
*.db-journal
*.db-wal
*.db-shm

# machine-local lifecycle config
launchd/
crontab.local
*.plist
# (repo docs: examples/lifecycle.plist is committed)

# editor-local
.opencode/
.cursor/

# Python build artifacts (pip install -e . creates these)
src/*.egg-info/

# OS
.DS_Store
Thumbs.db
